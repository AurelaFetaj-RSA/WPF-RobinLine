<UserControl x:Class="WPF_App.Views.AutomaticView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:viewModel="clr-namespace:WPF_App.ViewModels"
             xmlns:views="clr-namespace:WPF_App.Views"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:controls="http://schemas.xceed.com/wpf/xaml/toolkit"
             Width="700" Height="700">

    <UserControl.Resources>
        <Storyboard x:Key="RedLightBlinkStoryboard">
            <ColorAnimation Storyboard.TargetProperty="(Background).(SolidColorBrush.Color)"
                            From="DarkGreen" 
                            To="Red"
                            Duration="0:0:0.3"
                            AutoReverse="True"
                            RepeatBehavior="Forever"/>
        </Storyboard>
    </UserControl.Resources>

    <Viewbox Stretch="Uniform">
        <Grid>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border CornerRadius="10"
                        Background="Transparent"
                        Width="200"
                        Height="155"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Margin="-50,0,0,0">
                    <Border.RenderTransform>
                        <TranslateTransform Y="-85"/>
                        <!-- Moves Up -->
                    </Border.RenderTransform>

                    <Grid>
                        <Border Background="#f1f2f6"
                                CornerRadius="0,0,3,3"
                                Margin="-19,-11,19,11">
                            <Border.Effect>
                                <DropShadowEffect Color="Black"
                                                  BlurRadius="5"
                                                  ShadowDepth="1"
                                                  Opacity="0.6"/>
                            </Border.Effect>

                            <!-- Blue line at the top -->
                            <Grid>
                                <Rectangle x:Name="Robot1TopLine"
                                           Height="2"
                                           HorizontalAlignment="Stretch"
                                           VerticalAlignment="Top"
                                           Fill="#4471ce"
                                           RadiusX="3"
                                           RadiusY="3"
                                           Margin="0,-3,0,10"/>
                                <Rectangle RadiusX="6"
                                           RadiusY="6"
                                           Fill="#f2f3f5"/>
                            </Grid>
                        </Border>

                        <!-- TextBlock Positioned at Top-Left -->
                        <StackPanel HorizontalAlignment="Left" 
                                    VerticalAlignment="Top" 
                                    Margin="-10, -5, 0, 0">
                            <TextBlock x:Name="Robot1"
                                       Text="{DynamicResource Robot1}"
                                       Foreground="{DynamicResource titleColor7}"
                                       FontSize="15"
                                       FontWeight="Bold"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Top"
                                       Padding="5, 0"/>
                            <Border Height="1.5"
                                    Width="170"
                                    Background="#2a3645" />
                        </StackPanel>

                        <!--<Grid Margin="-5,25,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,4">
                                <Border x:Name="Robot1BeltSquare" Width="10" Height="10" Background="Gray" 
                                        CornerRadius="2" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="Belt" 
                                           Foreground="{DynamicResource titleColor7}"
                                           FontWeight="Bold" 
                                           FontSize="9"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,4">
                                <Border x:Name="Robot1TankLvlLowSquare" Width="10" Height="10" Background="Gray" 
                                        CornerRadius="2" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="Tank Low" 
                                           Foreground="{DynamicResource titleColor7}"
                                           FontWeight="Bold" 
                                           FontSize="9"/>
                            </StackPanel>

                            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,4">
                                <Border x:Name="Robot1TankLvlEmptySquare" Width="10" Height="10" Background="Gray" CornerRadius="2" 
                                        VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="Tank Empty" 
                                           Foreground="{DynamicResource titleColor7}"
                                           FontWeight="Bold" 
                                           FontSize="9"/>
                            </StackPanel>

                            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,4">
                                <Border x:Name="Robot1AirPressureSquare" Width="10" Height="10" Background="Gray" CornerRadius="2" 
                                        VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="Air Pressure" 
                                           Foreground="{DynamicResource titleColor7}"
                                           FontWeight="Bold" 
                                           FontSize="9"/>
                            </StackPanel>

                            <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,0,0,4">
                                <Border x:Name="Robot1AutomaticSquare" Width="10" Height="10" Background="Gray" CornerRadius="2" 
                                        VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="Automatic" 
                                           Foreground="{DynamicResource titleColor7}"
                                           FontWeight="Bold" 
                                           FontSize="9"/>
                            </StackPanel>

                        </Grid>-->

                        <Grid Margin="-8,55,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="110"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Column 0 -->
                            <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,4">
                                <Border x:Name="Robot1AirPressureSquare" Width="10" Height="10" Background="Gray" 
                                        CornerRadius="2" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="{DynamicResource AirPressure}"
                                               Foreground="{DynamicResource titleColor7}"
                                               FontWeight="Bold" 
                                               FontSize="9"/>
                            </StackPanel>

                            <StackPanel Grid.Column="0" Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,4">
                                <Border x:Name="Robot1TankLvlLowSquare" Width="10" Height="10" Background="Gray" 
                                        CornerRadius="2" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="{DynamicResource TankLow}" 
                                               Foreground="{DynamicResource titleColor7}"
                                               FontWeight="Bold" 
                                               FontSize="9"/>
                            </StackPanel>

                            <StackPanel Grid.Column="0" Grid.Row="2" Orientation="Horizontal" Margin="0,0,15,4">
                                <Border x:Name="Robot1TankLvlEmptySquare" Width="10" Height="10" Background="Gray" CornerRadius="2" 
                                        VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="{DynamicResource TankEmpty}" 
                                                Foreground="{DynamicResource titleColor7}"
                                                FontWeight="Bold" 
                                                FontSize="9"/>
                            </StackPanel>

                            <!-- Column 1 -->
                            <StackPanel Grid.Column="1" Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,4">
                                <Border x:Name="Robot1BeltSquare" Width="10" Height="10" Background="Gray" CornerRadius="2" 
                                        VerticalAlignment="Center" Margin="-10,0,8,0"/>
                                    <TextBlock Text="{DynamicResource  Belt}"
                                               Foreground="{DynamicResource titleColor7}"
                                               FontWeight="Bold" 
                                               FontSize="9"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,4">
                                <Border x:Name="Robot1AutomaticSquare" Width="10" Height="10" Background="Gray" CornerRadius="2" 
                                        VerticalAlignment="Center" Margin="-10,0,8,0"/>
                                    <TextBlock Text="{DynamicResource Automatic}" 
                                               Foreground="{DynamicResource titleColor7}"
                                               FontWeight="Bold" 
                                               FontSize="9"/>
                            </StackPanel>
                        </Grid>

                        <Border Width="25" Height="25"
                                CornerRadius="15"
                                Background="#f1f2f6"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Top"
                                Margin="140, 105, 0, 0">
                            <fa:IconImage x:Name="ReadyLamp"
                                          Icon="Lightbulb"
                                          Width="20"
                                          Height="20"
                                          Foreground="green"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Top"
                                    HorizontalAlignment="Center" Margin="100,125,0,0">
                            <TextBlock x:Name="ReadyRobot1TextBlock"
                                       Text="{DynamicResource Ready}"
                                       Foreground="{DynamicResource titleColor7}"
                                       FontWeight="Bold" 
                                       FontSize="12" 
                                       VerticalAlignment="Center"/>
                        </StackPanel>

                        <ToggleButton x:Name="Robot1Toggle"
                                      Width="35" Height="17" 
                                      HorizontalAlignment="Left"
                                      VerticalAlignment="Bottom"
                                      Margin="-10, 0, 0, 15"
                                      Background="Transparent"
                                      Cursor="Hand"
                                      BorderThickness="0"
                                      Click="Robot1Toggle_Click">
                            <ToggleButton.RenderTransform>
                                <TranslateTransform Y="-15"/>
                            </ToggleButton.RenderTransform>
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Grid Width="35" Height="17">
                                        <!-- Adjusted Grid size -->
                                        <!-- Background Border -->
                                        <Border x:Name="ToggleBackground"
                                                Width="35" Height="17" 
                                                CornerRadius="8"
                                                Background="Gray"/>

                                        <!-- Toggle Thumb -->
                                        <Ellipse x:Name="ToggleThumb"
                                                 Width="15" Height="15"  
                                                 Fill="White"
                                                 HorizontalAlignment="Left"
                                                 Margin="2, 0, 0, 0">
                                            <Ellipse.RenderTransform>
                                                <TranslateTransform x:Name="ThumbTransform"/>
                                            </Ellipse.RenderTransform>
                                        </Ellipse>
                                    </Grid>

                                    <!-- Storyboard Animation -->
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="ToggleBackground" Property="Background" Value="#3f73d1"/>
                                            <Setter Property="RenderTransform" TargetName="ToggleThumb">
                                                <Setter.Value>
                                                    <TranslateTransform X="16"/>
                                                    <!-- Adjusted movement -->
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                        <Trigger Property="IsChecked" Value="False">
                                            <Setter TargetName="ToggleBackground" Property="Background" Value="#d4dce5"/>
                                            <Setter Property="RenderTransform" TargetName="ToggleThumb">
                                                <Setter.Value>
                                                    <TranslateTransform X="0"/>
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                            <TextBlock x:Name="Robot1AvailabilityTextBlock"
                                       Text="{DynamicResource Inclusion}" 
                                       Margin="-20,115,0,0" 
                                       Foreground="{DynamicResource titleColor7}"
                                       FontWeight="Bold" 
                                       FontSize="12" 
                                       VerticalAlignment="Center"/>
                        </StackPanel>

                    </Grid>
                </Border>

                <Border CornerRadius="10"
                        Background="Transparent"
                        Width="200"
                        Height="155"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Margin="-70,40,0,0">
                    <Border.RenderTransform>
                        <TranslateTransform Y="50"/>
                        <!--<TranslateTransform X="40"/>-->
                        <!-- Moves Up -->
                    </Border.RenderTransform>

                    <Grid>
                        <Border Background="#f1f2f6"
                                CornerRadius="0,0,3,3"
                                Margin="-1,-11,1,11">
                            <Border.Effect>
                                <DropShadowEffect Color="Black"
                                                  BlurRadius="5"
                                                  ShadowDepth="1"
                                                  Opacity="0.6"/>
                            </Border.Effect>

                            <!-- Blue line at the top -->
                            <Grid>
                                <Rectangle x:Name="Robot2TopLine"
                                           Height="2"
                                           HorizontalAlignment="Stretch"
                                           VerticalAlignment="Top"
                                           Fill="#3f73d1"
                                           RadiusX="3"
                                           RadiusY="3"
                                           Margin="0,-3,0,10"/>
                                <Rectangle RadiusX="6"
                                           RadiusY="6"
                                           Fill="#f2f3f5"/>
                            </Grid>
                        </Border>

                        <!-- TextBlock Positioned at Top-Left -->
                        <StackPanel HorizontalAlignment="Left" 
                                    VerticalAlignment="Top" 
                                    Margin="10, -5, 0, 0">

                            <TextBlock Text="{DynamicResource Robot2}" 
                                       Foreground="{DynamicResource titleColor7}"
                                       FontSize="15"
                                       FontWeight="Bold"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Top"
                                       Padding="5, 0"/>
                            <Border Height="1.5"
                                    Width="170"
                                    Background="{DynamicResource titleColor7}" />
                        </StackPanel>

                        <Grid Margin="8,55,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="110"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Column 0 -->
                            <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Horizontal" Margin="0,0,15,4">
                                <Border x:Name="Robot2AirPressureSquare" Width="10" Height="10" Background="Gray" 
                                        CornerRadius="2" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="{DynamicResource AirPressure}"
                                               Foreground="{DynamicResource titleColor7}"
                                               FontWeight="Bold" 
                                               FontSize="9"/>
                            </StackPanel>

                            <StackPanel Grid.Column="0" Grid.Row="1" Orientation="Horizontal" Margin="0,0,15,4">
                                <Border x:Name="Robot2TankLvlLowSquare" Width="10" Height="10" Background="Gray" 
                                        CornerRadius="2" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="{DynamicResource TankLow}" 
                                           Foreground="{DynamicResource titleColor7}"
                                           FontWeight="Bold" 
                                           FontSize="9"/>
                            </StackPanel>

                            <StackPanel Grid.Column="0" Grid.Row="2" Orientation="Horizontal" Margin="0,0,15,4">
                                <Border x:Name="Robot2TankLvlEmptySquare" Width="10" Height="10" Background="Gray" CornerRadius="2" 
                                        VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="{DynamicResource TankEmpty}" 
                                           Foreground="{DynamicResource titleColor7}"
                                           FontWeight="Bold" 
                                           FontSize="9"/>
                            </StackPanel>

                            <!-- Column 1 -->
                            <StackPanel Grid.Column="1" Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,4">
                                <Border x:Name="Robot2BeltSquare" Width="10" Height="10" Background="Gray" CornerRadius="2" 
                                        VerticalAlignment="Center" Margin="-10,0,8,0"/>
                                    <TextBlock Text="{DynamicResource  Belt}"
                                               Foreground="{DynamicResource titleColor7}"
                                               FontWeight="Bold" 
                                               FontSize="9"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,4">
                                <Border x:Name="Robot2AutomaticSquare" Width="10" Height="10" Background="Gray" CornerRadius="2" 
                                        VerticalAlignment="Center" Margin="-10,0,8,0"/>
                                <TextBlock Text="{DynamicResource Automatic}" 
                                           Foreground="{DynamicResource titleColor7}"
                                           FontWeight="Bold" 
                                           FontSize="9"/>
                            </StackPanel>
                        </Grid>

                        <Border Width="25" Height="25"
                                CornerRadius="15"
                                Background="#f1f2f6"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Top"
                                Margin="160, 105, 0, 0">
                            <fa:IconImage x:Name="ReadyLamp2"
                                          Icon="Lightbulb"
                                          Width="20"
                                          Height="20"
                                          Foreground="green"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Center"/>
                        </Border>
                        <!--<StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                            <TextBlock x:Name="ReadyRobot2TextBlock"
                                       Text="{DynamicResource Ready}" 
                                       Margin="130,115,0,0" 
                                       Foreground="{DynamicResource titleColor7}" 
                                       FontWeight="Bold" 
                                       FontSize="12" 
                                       VerticalAlignment="Center">
                                <TextBlock.RenderTransform>
                                    <TranslateTransform X="5"/>
                                </TextBlock.RenderTransform>
                            </TextBlock>
                        </StackPanel>-->

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Top"
                                    HorizontalAlignment="Center" Margin="140,125,0,0">
                            <TextBlock x:Name="ReadyRobot2TextBlock"
                                       Text="{DynamicResource Ready}"
                                       Foreground="{DynamicResource titleColor7}"
                                       FontWeight="Bold" 
                                       FontSize="12" 
                                       VerticalAlignment="Center"/>
                        </StackPanel>

                        <ToggleButton x:Name="RobotToggle2"
                                      Width="35" Height="17" 
                                      HorizontalAlignment="Left"
                                      VerticalAlignment="Bottom"
                                      Margin="8, 0, 0, 15"
                                      Background="Transparent"
                                      Cursor="Hand"
                                      BorderThickness="0"
                                      Click="Robot2Toggle_Click">
                            <ToggleButton.RenderTransform>
                                <TranslateTransform Y="-15"/>
                            </ToggleButton.RenderTransform>
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Grid Width="35" Height="17">
                                        <!-- Adjusted Grid size -->
                                        <!-- Background Border -->
                                        <Border x:Name="ToggleBackground"
                                                Width="35" Height="17" 
                                                CornerRadius="8"
                                                Background="Gray"/>

                                        <!-- Toggle Thumb -->
                                        <Ellipse x:Name="ToggleThumb"
                                                 Width="15" Height="15"  
                                                 Fill="White"
                                                 HorizontalAlignment="Left"
                                                 Margin="2, 0, 0, 0">
                                            <Ellipse.RenderTransform>
                                                <TranslateTransform x:Name="ThumbTransform"/>
                                            </Ellipse.RenderTransform>
                                        </Ellipse>
                                    </Grid>

                                    <!-- Storyboard Animation -->
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="ToggleBackground" Property="Background" Value="#3f73d1"/>
                                            <Setter Property="RenderTransform" TargetName="ToggleThumb">
                                                <Setter.Value>
                                                    <TranslateTransform X="16"/>
                                                    <!-- Adjusted movement -->
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                        <Trigger Property="IsChecked" Value="False">
                                            <Setter TargetName="ToggleBackground" Property="Background" Value="#d4dce5"/>
                                            <Setter Property="RenderTransform" TargetName="ToggleThumb">
                                                <Setter.Value>
                                                    <TranslateTransform X="0"/>
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                            <TextBlock x:Name="Robot2AvailabilityTextBlock"
                                       Text="{DynamicResource Inclusion}" 
                                       Margin="0,115,0,0" 
                                       Foreground="{DynamicResource titleColor7}" 
                                       FontWeight="Bold" 
                                       FontSize="12" 
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border CornerRadius="10"
                        Background="Transparent"
                        Width="460"
                        Height="155"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Margin="170,0,0,0">
                    <Border.RenderTransform>
                        <TransformGroup>
                            <!--<TranslateTransform X="60" />-->
                            <TranslateTransform Y="-85" />
                        </TransformGroup>
                    </Border.RenderTransform>

                    <Grid>
                        <Border Background="#f1f2f6"
                                CornerRadius="0,0,3,3"
                                Margin="-19,-11,19,11">
                            <Border.Effect>
                                <DropShadowEffect Color="Black"
                                      BlurRadius="5"
                                      ShadowDepth="1"
                                      Opacity="0.6"/>
                            </Border.Effect>

                            <!-- Blue line at the top -->
                            <Grid>
                                <Rectangle x:Name="Oven1TopLine"
                                           Height="2"
                                           HorizontalAlignment="Stretch"
                                           VerticalAlignment="Top"
                                           Fill="#4471ce"
                                           RadiusX="3"
                                           RadiusY="3"
                                           Margin="0,-3,0,10"/>
                                <Rectangle RadiusX="6"
                                           RadiusY="6"
                                           Fill="#f2f3f5"/>
                            </Grid>
                        </Border>

                        <!-- TextBlock Positioned at Top-Left -->
                        <StackPanel HorizontalAlignment="Left" 
                                    VerticalAlignment="Top" 
                                    Margin="-5, -5, 0, 0">
                            <TextBlock Text="{DynamicResource Oven1}"
                                       Foreground="{DynamicResource titleColor7}"
                                       FontSize="15"
                                       FontWeight="Bold"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Top"
                                       Padding="5, 0"/>
                            <Border Height="1.5" 
                                    Width="420" 
                                    Background="{DynamicResource titleColor7}"/>
                        </StackPanel>

                        <Grid VerticalAlignment="Top" Margin="0,25,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <!-- Label (Lamps %, Temp Setpoint) -->
                                <ColumnDefinition Width="50"/>
                                <!-- Numeric control -->
                                <ColumnDefinition Width="170"/>
                                <!-- Label (Fan %, Temperature) -->
                                <ColumnDefinition Width="50"/>
                                <!-- Numeric control -->
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="5"/>
                                <!-- First row (Lamps/Fan %) -->
                                <RowDefinition Height="Auto"/>
                                <!-- Second row (Temp Setpoint/Temperature) -->
                            </Grid.RowDefinitions>

                            <!-- Row 0: Lamps Percentage & Fan Percentage -->
                            <TextBlock Text="{DynamicResource LampsPercentage}"
                                       Grid.Column="0" Grid.Row="0"
                                       Foreground="{DynamicResource titleColor7}"
                                       FontWeight="Bold" 
                                       FontSize="12"
                                       VerticalAlignment="Center"/>

                            <controls:IntegerUpDown x:Name="Oven1LampsPercentageUpDown"
                                                    Grid.Column="1" Grid.Row="0"
                                                    Height="25"
                                                    FontSize="15"
                                                    FontWeight="Bold"
                                                    VerticalAlignment="Center"
                                                    Increment="1"
                                                    Value="0"
                                                    Minimum="0"
                                                    Maximum="100"
                                                    ValueChanged="Oven1LampsPercentageUpDown_ValueChanged"/>

                            <TextBlock Text="{DynamicResource FanPercentage}"
                                       Grid.Column="2" Grid.Row="0"
                                       Margin="20,0,0,0"
                                       Foreground="{DynamicResource titleColor7}"
                                       FontWeight="Bold"
                                       FontSize="12"
                                       VerticalAlignment="Center"/>

                            <controls:IntegerUpDown x:Name="Oven1FanPercentageUpDown"
                                                    Grid.Column="3" Grid.Row="0"
                                                    Height="25"
                                                    FontSize="15"
                                                    FontWeight="Bold"
                                                    VerticalAlignment="Center"
                                                    Increment="1"
                                                    Value="0"
                                                    Minimum="0"
                                                    Maximum="100"
                                                    ValueChanged="Oven1FanPercentageUpDown_ValueChanged"/>
                            
                            <!-- Row 1: Temperature Setpoint & Temperature -->
                            <TextBlock Text="{DynamicResource TemperatureSetpoint}"
                                       Grid.Column="0" Grid.Row="2"
                                       Foreground="{DynamicResource titleColor7}"
                                       FontWeight="Bold"
                                       FontSize="12"
                                       VerticalAlignment="Center"/>
                            
                            <controls:IntegerUpDown x:Name="Oven1TempSetpointUpDown"
                                                    Grid.Column="1" Grid.Row="2"
                                                    Height="25"
                                                    FontSize="15"
                                                    FontWeight="Bold"
                                                    VerticalAlignment="Center"
                                                    Increment="1"
                                                    Value="0"
                                                    Minimum="0"
                                                    Maximum="100"
                                                    ValueChanged="Oven1TempSetpointUpDown_ValueChanged"/>

                            <TextBlock Text="{DynamicResource Temperature}"
                                       Grid.Column="2" Grid.Row="2"
                                       Margin="20,0,0,0"  
                                       Foreground="{DynamicResource titleColor7}"
                                       FontWeight="Bold"
                                       FontSize="12"
                                       VerticalAlignment="Center"/>

                            <TextBlock x:Name="TemperatureTextBlock"
                                       Grid.Column="3" Grid.Row="2"
                                       Foreground="{DynamicResource titleColor7}"
                                       FontWeight="Bold"
                                       FontSize="15"
                                       VerticalAlignment="Center"/>
                        </Grid>

                        <Border Name="StatusBorder"
                                Width="22"
                                Height="22"
                                CornerRadius="15"
                                Background="#f1f2f6"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Top"
                                Margin="275, 105, 0, 0">
                            <!--<fa:IconImage Name="StatusIcon"
                                          Icon="Check"
                                          Width="17"
                                          Height="17"
                                          Foreground="green"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Center"/>-->
                            <Ellipse x:Name="EllipseIcon" Width="25" Height="25" Fill="Green" Margin="-5,-5,0,0"/>
                        </Border>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Top"
                                    HorizontalAlignment="Center" Margin="90,125,0,0">
                            <TextBlock x:Name="ReachedTextBlock"
                                       Text="{DynamicResource TemperatureOk}"
                                       Foreground="{DynamicResource titleColor7}"
                                       FontWeight="Bold" 
                                       FontSize="12" 
                                       VerticalAlignment="Center"/>
                        </StackPanel>

                        <Border Width="65" Height="60"
                                Background="Transparent"
                                BorderThickness="0"
                                Margin="350,90,0,0"
                                VerticalAlignment="Top">
                            <Border.RenderTransform>
                                <TranslateTransform Y="0"/>
                                <!--Moves Up-->
                            </Border.RenderTransform>

                            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <!-- Icon (Ellipse) -->
                                <Ellipse x:Name="OvenStateIcon" Width="25" Height="25" Fill="White" Margin="5,0,0,0"/>

                                <!-- State Text below the icon -->
                                <TextBlock x:Name="OvenStateText" 
                                           Text="{DynamicResource State}"
                                           Foreground="{DynamicResource titleColor7}" 
                                           FontSize="12" 
                                           FontWeight="Bold" 
                                           VerticalAlignment="Center" 
                                           HorizontalAlignment="Center" 
                                           Margin="5,0,0,0"/>
                            </StackPanel>

                        </Border>

                        <Border Width="25" Height="25"
                                CornerRadius="15"
                                Background="#f1f2f6"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Top"
                                Margin="335, 105, 0, 0">
                            <fa:IconImage x:Name="LampIcon"
                                          Icon="Lightbulb"
                                          Width="20"
                                          Height="20"
                                          Foreground="green"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Top"
                                    HorizontalAlignment="Center" Margin="235,125,0,0">
                            <TextBlock x:Name="ReadyOven1TextBlock"
                                       Text="{DynamicResource Ready}"
                                       Foreground="{DynamicResource titleColor7}"
                                       FontWeight="Bold" 
                                       FontSize="12" 
                                       VerticalAlignment="Center"/>
                        </StackPanel>

                        <ToggleButton x:Name="Oven1Toggle"
                                      Width="35" Height="17" 
                                      HorizontalAlignment="Left"
                                      VerticalAlignment="Bottom"
                                      Margin="-5, 0, 0, 15"
                                      Background="Transparent"
                                      Cursor="Hand"
                                      BorderThickness="0"
                                      Click="Oven1Toggle_Click">
                            <ToggleButton.RenderTransform>
                                <TranslateTransform Y="-15"/>
                            </ToggleButton.RenderTransform>
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Grid Width="35" Height="17">
                                        <!-- Adjusted Grid size -->
                                        <!-- Background Border -->
                                        <Border x:Name="ToggleBackground"
                                                Width="35" Height="17" 
                                                CornerRadius="8"
                                                Background="Gray"/>

                                        <!-- Toggle Thumb -->
                                        <Ellipse x:Name="ToggleThumb"
                                                 Width="15" Height="15"  
                                                 Fill="White"
                                                 HorizontalAlignment="Left"
                                                 Margin="2, 0, 0, 0">
                                            <Ellipse.RenderTransform>
                                                <TranslateTransform x:Name="ThumbTransform"/>
                                            </Ellipse.RenderTransform>
                                        </Ellipse>
                                    </Grid>

                                    <!-- Storyboard Animation -->
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="ToggleBackground" Property="Background" Value="#3f73d1"/>
                                            <Setter Property="RenderTransform" TargetName="ToggleThumb">
                                                <Setter.Value>
                                                    <TranslateTransform X="16"/>
                                                    <!-- Adjusted movement -->
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                        <Trigger Property="IsChecked" Value="False">
                                            <Setter TargetName="ToggleBackground" Property="Background" Value="#d4dce5"/>
                                            <Setter Property="RenderTransform" TargetName="ToggleThumb">
                                                <Setter.Value>
                                                    <TranslateTransform X="0"/>
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                            <TextBlock x:Name="AvailabilityTextBlock" 
                                       Text="{DynamicResource Inclusion}"
                                       Margin="-15,115,0,0" 
                                       Foreground="{DynamicResource titleColor7}" 
                                       FontWeight="Bold" 
                                       FontSize="12" 
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border CornerRadius="10"
                        Background="Transparent"
                        Width="460"
                        Height="155"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Margin="170,40,0,0">
                    <Border.RenderTransform>
                        <TranslateTransform Y="50"/>
                        <!-- Moves Up -->
                    </Border.RenderTransform>

                    <Grid>
                        <Border Background="#f1f2f6"
                                CornerRadius="0,0,3,3"
                                Margin="-19,-11,19,11">
                            <Border.Effect>
                                <DropShadowEffect Color="Black"
                                                  BlurRadius="5"
                                                  ShadowDepth="1"
                                                  Opacity="0.6"/>
                            </Border.Effect>

                            <!-- Blue line at the top -->
                            <Grid>
                                <Rectangle x:Name="Oven2TopLine"
                                           Height="2"
                                           HorizontalAlignment="Stretch"
                                           VerticalAlignment="Top"
                                           Fill="#4471ce"
                                           RadiusX="3"
                                           RadiusY="3"
                                           Margin="0,-3,0,10"/>
                                <Rectangle RadiusX="6"
                                           RadiusY="6"
                                           Fill="#f2f3f5"/>
                            </Grid>
                        </Border>

                        <StackPanel HorizontalAlignment="Left" 
                                    VerticalAlignment="Top" 
                                    Margin="-5, -5, 0, 0">
                            <TextBlock Text="{DynamicResource Oven2}"
                                       Foreground="{DynamicResource titleColor7}"
                                       FontSize="15"
                                       FontWeight="Bold"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Top"
                                       Padding="5, 0"/>
                            <Border Height="1.5"
                                    Width="420"
                                    Background="{DynamicResource titleColor7}" />
                        </StackPanel>

                        <Grid VerticalAlignment="Top" Margin="0,25,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <!-- Label (Lamps %, Temp Setpoint) -->
                                <ColumnDefinition Width="50"/>
                                <!-- Numeric control -->
                                <ColumnDefinition Width="170"/>
                                <!-- Label (Fan %, Temperature) -->
                                <ColumnDefinition Width="50"/>
                                <!-- Numeric control -->
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="5"/>
                                <!-- First row (Lamps/Fan %) -->
                                <RowDefinition Height="Auto"/>
                                <!-- Second row (Temp Setpoint/Temperature) -->
                            </Grid.RowDefinitions>

                            <!-- Row 0: Lamps Percentage & Fan Percentage -->
                            <TextBlock Text="{DynamicResource LampsPercentage}"
                                       Grid.Column="0" Grid.Row="0"
                                       Foreground="{DynamicResource titleColor7}"
                                       FontWeight="Bold" 
                                       FontSize="12"
                                       VerticalAlignment="Center"/>

                            <controls:IntegerUpDown x:Name="Oven2LampsPercentageUpDown"
                                                    Grid.Column="1" Grid.Row="0"
                                                    Height="25"
                                                    FontSize="15"
                                                    FontWeight="Bold"
                                                    VerticalAlignment="Center"
                                                    Increment="1"
                                                    Value="0"
                                                    Minimum="0"
                                                    Maximum="100"
                                                    ValueChanged="Oven2LampsPercentageUpDown_ValueChanged"/>

                            <TextBlock Text="{DynamicResource FanPercentage}"
                                       Grid.Column="2" Grid.Row="0"
                                       Margin="20,0,0,0"
                                       Foreground="{DynamicResource titleColor7}"
                                       FontWeight="Bold"
                                       FontSize="12"
                                       VerticalAlignment="Center"/>

                            <controls:IntegerUpDown x:Name="Oven2FanPercentageUpDown"
                                                    Grid.Column="3" Grid.Row="0"
                                                    Height="25"
                                                    FontSize="15"
                                                    FontWeight="Bold"
                                                    VerticalAlignment="Center"
                                                    Increment="1"
                                                    Value="0"
                                                    Minimum="0"
                                                    Maximum="100"
                                                    ValueChanged="Oven2FanPercentageUpDown_ValueChanged"/>

                            <!-- Row 1: Temperature Setpoint & Temperature -->
                            <TextBlock Text="{DynamicResource TemperatureSetpoint}"
                                       Grid.Column="0" Grid.Row="2"
                                       Foreground="{DynamicResource titleColor7}"
                                       FontWeight="Bold"
                                       FontSize="12"
                                       VerticalAlignment="Center"/>

                            <controls:IntegerUpDown x:Name="Oven2TempSetpointUpDown"
                                                    Grid.Column="1" Grid.Row="2"
                                                    Height="25"
                                                    FontSize="15"
                                                    FontWeight="Bold"
                                                    VerticalAlignment="Center"
                                                    Increment="1"
                                                    Value="0"
                                                    Minimum="0"
                                                    Maximum="100"
                                                    ValueChanged="Oven2TempSetpointUpDown_ValueChanged"/>

                            <TextBlock Text="{DynamicResource Temperature}"
                                       Grid.Column="2" Grid.Row="2"
                                       Margin="20,0,0,0"  
                                       Foreground="{DynamicResource titleColor7}"
                                       FontWeight="Bold"
                                       FontSize="12"
                                       VerticalAlignment="Center"/>

                            <TextBlock x:Name="Oven2TemperatureTextBlock"
                                       Grid.Column="3" Grid.Row="2"
                                       Foreground="{DynamicResource titleColor7}"
                                       FontWeight="Bold"
                                       FontSize="15"
                                       VerticalAlignment="Center"/>
                        </Grid>

                        <Border Width="65" Height="60"
                                Background="Transparent"
                                BorderThickness="0"
                                Margin="350,80,0,0"
                                VerticalAlignment="Top">
                            <Border.RenderTransform>
                                <TranslateTransform Y="10"/>
                                <!--Moves Up-->
                            </Border.RenderTransform>

                            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <!-- Icon (Ellipse) -->
                                <Ellipse x:Name="Oven2StateIcon" Width="25" Height="25" Fill="White" Margin="5,0,0,0"/>

                                <!-- State Text below the icon -->
                                <TextBlock x:Name="Oven2StateText" 
                                           Text="{DynamicResource State}"
                                           Foreground="{DynamicResource titleColor7}"
                                           FontSize="12" 
                                           FontWeight="Bold" 
                                           VerticalAlignment="Center" 
                                           HorizontalAlignment="Center" 
                                           Margin="5,0,0,0"/>
                            </StackPanel>

                        </Border>

                        <Border Name="Oven2StatusBorder"
                                Width="22" 
                                Height="22"
                                CornerRadius="15"
                                Background="#f1f2f6"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Top"
                                Margin="275, 105, 0, 0">
                            <!--<fa:IconImage Name="Oven2StatusIcon"
                                          Icon="Times"
                                          Width="14"
                                          Height="14"
                                          Foreground="red"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Center"/>-->
                            <Ellipse x:Name="Oven2EllipseIcon" Width="25" Height="25" Fill="Green" Margin="-5,-5,0,0"/>
                        </Border>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Top"
                                    HorizontalAlignment="Center" Margin="90,125,0,0">
                            <TextBlock x:Name="Oven2ReachedTextBlock"
                                       Text="{DynamicResource TemperatureOk}"
                                       Foreground="{DynamicResource titleColor7}"
                                       FontWeight="Bold" 
                                       FontSize="12" 
                                       VerticalAlignment="Center"/>
                        </StackPanel>

                        <Border Width="25" Height="25"
                                CornerRadius="15"
                                Background="#f1f2f6"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Top"
                                Margin="335, 105, 0, 0">
                            <fa:IconImage x:Name="Oven2LampIcon"
                                          Icon="Lightbulb"
                                          Width="20"
                                          Height="20"
                                          Foreground="green"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Top"
                                    HorizontalAlignment="Center" Margin="235,125,0,0">
                            <TextBlock x:Name="ReadyOven2TextBlock"
                                       Text="{DynamicResource Ready}"
                                       Foreground="{DynamicResource titleColor7}"
                                       FontWeight="Bold" 
                                       FontSize="12" 
                                       VerticalAlignment="Center"/>
                        </StackPanel>

                        <ToggleButton x:Name="Oven2Toggle"
                                      Width="35" Height="17" 
                                      HorizontalAlignment="Left"
                                      VerticalAlignment="Bottom"
                                      Margin="-5, 0, 0, 15"
                                      Background="Transparent"
                                      Cursor="Hand"
                                      BorderThickness="0"
                                      Click="Oven2Toggle_Click">
                            <ToggleButton.RenderTransform>
                                <TranslateTransform Y="-15"/>
                            </ToggleButton.RenderTransform>
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Grid Width="35" Height="17">
                                        <!-- Adjusted Grid size -->
                                        <!-- Background Border -->
                                        <Border x:Name="ToggleBackground"
                                                Width="35" Height="17" 
                                                CornerRadius="8"
                                                Background="Gray"/>

                                        <!-- Toggle Thumb -->
                                        <Ellipse x:Name="ToggleThumb"
                                                 Width="15" Height="15"  
                                                 Fill="White"
                                                 HorizontalAlignment="Left"
                                                 Margin="2, 0, 0, 0">
                                            <Ellipse.RenderTransform>
                                                <TranslateTransform x:Name="ThumbTransform"/>
                                            </Ellipse.RenderTransform>
                                        </Ellipse>
                                    </Grid>

                                    <!-- Storyboard Animation -->
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="ToggleBackground" Property="Background" Value="#3f73d1"/>
                                            <Setter Property="RenderTransform" TargetName="ToggleThumb">
                                                <Setter.Value>
                                                    <TranslateTransform X="16"/>
                                                    <!-- Adjusted movement -->
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                        <Trigger Property="IsChecked" Value="False">
                                            <Setter TargetName="ToggleBackground" Property="Background" Value="#d4dce5"/>
                                            <Setter Property="RenderTransform" TargetName="ToggleThumb">
                                                <Setter.Value>
                                                    <TranslateTransform X="0"/>
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                            <TextBlock x:Name="Availability2TextBlock" 
                                       Text="{DynamicResource Inclusion}"
                                       Margin="-15,115,0,0" 
                                       Foreground="{DynamicResource titleColor7}"
                                       FontWeight="Bold" 
                                       FontSize="12" 
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border CornerRadius="10"
                        Background="Transparent"
                        Width="225"
                        Height="130"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Margin="-70,265,0,0">
                    <Border.RenderTransform>
                        <TransformGroup>
                            <!--<TranslateTransform X="60" />-->
                            <!--<TranslateTransform Y="-45" />-->
                        </TransformGroup>
                    </Border.RenderTransform>

                    <Grid>
                        <Border Background="#f1f2f6"
                                CornerRadius="0,0,3,3"
                                Margin="-1,-11,1,11">
                            <Border.Effect>
                                <DropShadowEffect Color="Black"
                                                  BlurRadius="5"
                                                  ShadowDepth="1"
                                                  Opacity="0.6"/>
                            </Border.Effect>

                            <!-- Blue line at the top -->
                            <Grid>
                                <Rectangle Height="2"
                                           HorizontalAlignment="Stretch"
                                           VerticalAlignment="Top"
                                           Fill="#3f73d1"
                                           RadiusX="3"
                                           RadiusY="3"
                                           Margin="0,-3,0,10"/>
                                <Rectangle RadiusX="6"
                                           RadiusY="6"
                                           Fill="#f2f3f5"/>
                            </Grid>
                        </Border>

                        <!-- TextBlock Positioned at Top-Left -->
                        <StackPanel HorizontalAlignment="Left" 
                                    VerticalAlignment="Top" 
                                    Margin="10, -5, 0, 0">

                            <TextBlock Text="{DynamicResource GeneralStatus}"
                                       Foreground="{DynamicResource titleColor7}"
                                       FontSize="15"
                                       FontWeight="Bold"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Top"
                                       Padding="5, 0"/>
                            <Border Height="1.5"
                                    Width="200"
                                    Background="{DynamicResource titleColor7}" />
                        </StackPanel>

                        <Grid Margin="10,23,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Column 0 -->
                            <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Horizontal" Margin="0,78,15,4">
                                <Border x:Name="InputBeltSquare" Width="10" Height="10" Background="Gray" 
                                        CornerRadius="2" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="{DynamicResource InputBelt}"
                                               Foreground="{DynamicResource titleColor7}"
                                               FontWeight="Bold" 
                                               FontSize="9"/>
                            </StackPanel>

                            <!-- Column 1 -->
                            <StackPanel Grid.Column="1" Grid.Row="0" Orientation="Horizontal" Margin="10,78,0,4">
                                <Border x:Name="CentralBeltSquare" Width="10" Height="10" Background="Gray" CornerRadius="2" 
                                        VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="{DynamicResource CentralBelt}"
                                               Foreground="{DynamicResource titleColor7}"
                                               FontWeight="Bold" 
                                               FontSize="9"/>
                            </StackPanel>
                        </Grid>

                        <Border x:Name= "RedLight"
                                Width="18" Height="18"
                                CornerRadius="15"
                                Background="Darkgreen"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Top"
                                Margin="195, 25, 0, 0">
                        </Border>
                        <Border x:Name="OrangeLight"
                                Width="18" Height="18"
                                CornerRadius="15"
                                Background="DarkGreen"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Top"
                                Margin="195, 50, 0, 0">
                        </Border>
                        <Border x:Name="GreenLight"
                                Width="18" Height="18"
                                CornerRadius="15"
                                Background="DarkGreen"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Top"
                                Margin="195, 75, 0, 0">
                        </Border>

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                            <TextBlock x:Name="Selector" 
                                       Text="{DynamicResource SelectorInAutomatic}"
                                       Margin="0,58,0,0" 
                                       Foreground="{DynamicResource titleColor7}" 
                                       FontWeight="Bold" 
                                       FontSize="12" 
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                            <TextBlock x:Name="Line"
                                       Text="{DynamicResource LineStarted}"
                                       Margin="0,40,0,0" 
                                       Foreground="{DynamicResource titleColor7}" 
                                       FontWeight="Bold" 
                                       FontSize="12" 
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                            <TextBlock x:Name="Emergency" 
                                       Text="{DynamicResource EmergencyOk}"
                                       Margin="0,20,0,0" 
                                       Foreground="{DynamicResource titleColor7}" 
                                       FontWeight="Bold" 
                                       FontSize="12" 
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border CornerRadius="10"
                        Background="Transparent"
                        Width="435"
                        Height="155"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Margin="190,180,0,0">
                    <Grid>

                        <Border Background="#f1f2f6"
                                CornerRadius="0,0,3,3"
                                Margin="-19,73,15,-50">
                            <Border.Effect>
                                <DropShadowEffect Color="Black"
                                      BlurRadius="5"
                                      ShadowDepth="1"
                                      Opacity="0.6"/>
                            </Border.Effect>

                            <!-- Blue line at the top -->
                            <Grid>
                                <Rectangle Height="2"
                                           HorizontalAlignment="Stretch"
                                           VerticalAlignment="Top"
                                           Fill="#4471ce"
                                           RadiusX="3"
                                           RadiusY="3"
                                           Margin="0,-3,0,10"/>
                                <Rectangle RadiusX="6"
                                           RadiusY="6"
                                           Fill="#f2f3f5"/>
                            </Grid>
                        </Border>

                        <!-- TextBlock Positioned at Top-Left -->
                        <StackPanel HorizontalAlignment="Left" 
                                    VerticalAlignment="Top" 
                                    Margin="-10, 80, 0, 0">

                            <TextBlock Text="{DynamicResource GeneralControl}"
                                       Foreground="{DynamicResource titleColor7}"
                                       FontSize="15"
                                       FontWeight="Bold"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Top"
                                       Padding="5, 0"/>
                            <Border Height="1.5"
                                    Width="400"
                                    Background="{DynamicResource titleColor7}" />
                        </StackPanel>

                        <ComboBox x:Name="BeltSpeedComboBox"
                                  SelectionChanged="BeltSpeedComboBox_SelectionChanged"
                                  Width="90"
                                  Height="22"
                                  Margin="-140,10,0,0"
                                  SelectedIndex="1"
                                  FontSize="12"
                                  FontWeight="Bold">
                            <ComboBox.RenderTransform>
                                <TranslateTransform Y="42"/>
                                <!-- Moves Up -->
                            </ComboBox.RenderTransform>
                            <!--<ComboBoxItem Content="Slow Speed" Tag="1"/>
                            <ComboBoxItem Content="Medium Speed" Tag="2" IsSelected="True"/>
                            <ComboBoxItem Content="High Speed" Tag="3"/>-->

                            <ComboBoxItem Tag="1">
                                <TextBlock Text="{DynamicResource SlowSpeed}" />
                            </ComboBoxItem>
                            <ComboBoxItem Tag="2" IsSelected="True">
                                <TextBlock Text="{DynamicResource MediumSpeed}" />
                            </ComboBoxItem>
                            <ComboBoxItem Tag="3">
                                <TextBlock Text="{DynamicResource HighSpeed}" />
                            </ComboBoxItem>
                        </ComboBox>

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                            <TextBlock Text="{DynamicResource BeltSpeed}"
                                       Margin="-18,50,0,0" 
                                       Foreground="{DynamicResource titleColor7}" 
                                       FontWeight="Bold" 
                                       FontSize="12" 
                                       VerticalAlignment="Center">
                                <TextBlock.RenderTransform>
                                    <TransformGroup>
                                        <!--<TranslateTransform X="60" />-->
                                        <TranslateTransform Y="55" />
                                    </TransformGroup>
                                </TextBlock.RenderTransform>
                            </TextBlock>
                        </StackPanel>

                        <Border Width="65" Height="60"
                                Background="Transparent"
                                BorderThickness="0"
                                Margin="-390,80,0,0"
                                VerticalAlignment="Top">
                            <Border.RenderTransform>
                                <TranslateTransform Y="65"/>
                                <!--Moves Up-->
                            </Border.RenderTransform>

                            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <!-- Icon (Ellipse) -->
                                <Ellipse x:Name="StateIcon" Width="30" Height="30" Fill="#3f73d1" Margin="0,0,0,0"/>

                                <!-- State Text below the icon -->
                                <TextBlock x:Name="StateText" 
                                           Text="{DynamicResource State}"
                                           Foreground="{DynamicResource titleColor7}"
                                           FontSize="12" 
                                           FontWeight="Bold" 
                                           VerticalAlignment="Center" 
                                           HorizontalAlignment="Center" 
                                           Margin="0,5,0,0"/>
                            </StackPanel>

                        </Border>

                        <ComboBox x:Name="ModelNamesComboBox"
                                  Width="80"
                                  Height="22"
                                  FontSize="10"
                                  Margin="90,0,0,0"
                                  VerticalAlignment="Center"
                                  DisplayMemberPath="."
                                  SelectedValuePath=".">
                            <ComboBox.RenderTransform>
                                <TranslateTransform Y="47"/>
                            </ComboBox.RenderTransform>
                        </ComboBox>

                        <Button Content="{DynamicResource UpdateBuffer}" 
                                Click="ButtonUpdateRotary_Click" 
                                Width="110" Height="22"
                                Margin="287,0,0,0">
                            <Button.RenderTransform>
                                <TranslateTransform Y="47"/>
                            </Button.RenderTransform>
                        </Button>

                        <Button Content="{DynamicResource ResetBuffer}"
                                Click="ButtonResetRotary_Click" 
                                Width="110" Height="22"
                                Margin="287,50,0,0">
                            <Button.RenderTransform>
                                <TranslateTransform Y="55"/>
                            </Button.RenderTransform>
                        </Button>
                    </Grid>
                </Border>

                <Border x:Name="MessageBoxPanel"
                        CornerRadius="5"
                        Background="Transparent"
                        Width="380"
                        Height="30"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Visibility="Collapsed"
                        Margin="60,240,0,0">
                    <Border.RenderTransform>
                        <TransformGroup>
                            <TranslateTransform X="180" />
                            <TranslateTransform Y="150" />
                        </TransformGroup>
                    </Border.RenderTransform>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal" Grid.Column="0">
                            <Image x:Name="MessageIcon" 
                                   Width="15" 
                                   Height="15" 
                                   Margin="10,5,5,5"/>
                            <TextBlock x:Name="MessageText" 
                                       FontSize="12" 
                                       VerticalAlignment="Center" 
                                       Margin="5,5,20,5" 
                                       TextWrapping="Wrap"/>
                        </StackPanel>

                        <Button Content="✖"
                                Background="Transparent"
                                Foreground="Gray"
                                BorderThickness="0"
                                FontSize="8"
                                Cursor="Hand"
                                Margin="5,0,10,0"
                                Click="CloseMessageBox"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Grid.Column="1"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Viewbox>
</UserControl>