<UserControl x:Class="WPF_App.Views.ManualView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:viewModel="clr-namespace:WPF_App.ViewModels"
             xmlns:views="clr-namespace:WPF_App.Views"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:controls="http://schemas.xceed.com/wpf/xaml/toolkit"
             Height="300" Width="500">
    <Grid>
        <Border CornerRadius="10"
                Background="Transparent"
                Width="230"
                Height="170"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="0,0,0,0">
            <Border.RenderTransform>
                <TranslateTransform Y="-40"/>
                <!-- Moves Up -->
            </Border.RenderTransform>

            <Grid>
                <!-- Dashed Border -->
                <Rectangle Stroke="White"
                           StrokeThickness="0.5"
                           StrokeDashArray="6 6"
                           RadiusX="10"
                           RadiusY="10"
                           Fill="Transparent" Margin="-19,-11,19,11"/>

                <!-- TextBlock Positioned at Top-Left -->
                <StackPanel Background="#157291"
                            HorizontalAlignment="Left" 
                            VerticalAlignment="Top" 
                            Margin="-5, -20, 0, 0">
                    <TextBlock Text="Oven 1"
                               Foreground="White"
                               FontSize="15"
                               FontWeight="Bold"
                               HorizontalAlignment="Left"
                               VerticalAlignment="Top"
                               Padding="5, 0"/>
                </StackPanel>
                

                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                    <TextBlock Text="Lamps" 
                               Margin="-16,5,0,0" 
                               Foreground="White" 
                               FontWeight="Bold" 
                               FontSize="12" 
                               VerticalAlignment="Center"/>
                </StackPanel>

                <ToggleButton x:Name="Oven1LampsToggle"
                              Width="40" Height="17" 
                              HorizontalAlignment="Left"
                              VerticalAlignment="Bottom"
                              Margin="85, 0, 0, 15"
                              Background="Transparent"
                              Cursor="Hand"
                              BorderThickness="0"
                              Click="Oven1LampsToggle_Click">
                    <ToggleButton.RenderTransform>
                        <TranslateTransform Y="-123"/>
                    </ToggleButton.RenderTransform>
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="ToggleButton">
                            <Grid Width="40" Height="17">
                                <!-- Adjusted Grid size -->
                                <!-- Background Border -->
                                <Border x:Name="ToggleBackground"
                                        Width="40" Height="17" 
                                        CornerRadius="8"
                                        Background="Gray"/>

                                <!-- Toggle Thumb -->
                                <Ellipse x:Name="ToggleThumb"
                                         Width="15" Height="15"  
                                         Fill="White"
                                         HorizontalAlignment="Left"
                                         Margin="2, 0, 0, 0">
                                    <Ellipse.RenderTransform>
                                        <TranslateTransform x:Name="ThumbTransform"/>
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                            </Grid>

                            <!-- Storyboard Animation -->
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="ToggleBackground" Property="Background" Value="#02a29a"/>
                                    <Setter Property="RenderTransform" TargetName="ToggleThumb">
                                        <Setter.Value>
                                            <TranslateTransform X="20"/>
                                            <!-- Adjusted movement -->
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="False">
                                    <Setter TargetName="ToggleBackground" Property="Background" Value="#d0dae1"/>
                                    <Setter Property="RenderTransform" TargetName="ToggleThumb">
                                        <Setter.Value>
                                            <TranslateTransform X="0"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>
                
                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                    <TextBlock x:Name="LampsTextBlock" 
                               Text="On/Off"
                               Margin="125, 5, 0, 15"
                               Foreground="White" 
                               FontWeight="Bold" 
                               FontSize="12" 
                               VerticalAlignment="Center"/>
                </StackPanel>


                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                    <TextBlock Text="Fans" 
                               Margin="-16,35,0,0" 
                               Foreground="White" 
                               FontWeight="Bold" 
                               FontSize="12" 
                               VerticalAlignment="Center"/>
                </StackPanel>
                
                <ToggleButton x:Name="Oven1FansToggle"
                              Width="40" Height="17" 
                              HorizontalAlignment="Left"
                              VerticalAlignment="Bottom"
                              Margin="85, 0, 0, 15"
                              Background="Transparent"
                              Cursor="Hand"
                              BorderThickness="0"
                              Click="Oven1FansToggle_Click">
                    <ToggleButton.RenderTransform>
                        <TranslateTransform Y="-93"/>
                    </ToggleButton.RenderTransform>
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="ToggleButton">
                            <Grid Width="40" Height="17">
                                <!-- Adjusted Grid size -->
                                <!-- Background Border -->
                                <Border x:Name="ToggleBackground"
                                        Width="40" Height="17" 
                                        CornerRadius="8"
                                        Background="Gray"/>

                                <!-- Toggle Thumb -->
                                <Ellipse x:Name="ToggleThumb"
                                         Width="15" Height="15"  
                                         Fill="White"
                                         HorizontalAlignment="Left"
                                         Margin="2, 0, 0, 0">
                                    <Ellipse.RenderTransform>
                                        <TranslateTransform x:Name="ThumbTransform"/>
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                            </Grid>

                            <!-- Storyboard Animation -->
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="ToggleBackground" Property="Background" Value="#02a29a"/>
                                    <Setter Property="RenderTransform" TargetName="ToggleThumb">
                                        <Setter.Value>
                                            <TranslateTransform X="20"/>
                                            <!-- Adjusted movement -->
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="False">
                                    <Setter TargetName="ToggleBackground" Property="Background" Value="#d0dae1"/>
                                    <Setter Property="RenderTransform" TargetName="ToggleThumb">
                                        <Setter.Value>
                                            <TranslateTransform X="0"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>
                
                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                    <TextBlock x:Name="FansTextBlock" 
                               Text="On/Off"
                               Margin="125, 35, 0, 15"
                               Foreground="White" 
                               FontWeight="Bold" 
                               FontSize="12" 
                               VerticalAlignment="Center"/>
                </StackPanel>


                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                    <TextBlock Text="Belt" 
                               Margin="-16,65,0,0" 
                               Foreground="White" 
                               FontWeight="Bold" 
                               FontSize="12" 
                               VerticalAlignment="Center"/>
                </StackPanel>

                <ToggleButton x:Name="Oven1BeltToggle"
                              Width="40" Height="17" 
                              HorizontalAlignment="Left"
                              VerticalAlignment="Bottom"
                              Margin="85, 0, 0, 15"
                              Background="Transparent"
                              Cursor="Hand"
                              BorderThickness="0"
                              Click="Oven1BeltToggle_Click">
                    <ToggleButton.RenderTransform>
                        <TranslateTransform Y="-63"/>
                    </ToggleButton.RenderTransform>
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="ToggleButton">
                            <Grid Width="40" Height="17">
                                <!-- Adjusted Grid size -->
                                <!-- Background Border -->
                                <Border x:Name="ToggleBackground"
                                        Width="40" Height="17" 
                                        CornerRadius="8"
                                        Background="Gray"/>

                                <!-- Toggle Thumb -->
                                <Ellipse x:Name="ToggleThumb"
                                         Width="15" Height="15"  
                                         Fill="White"
                                         HorizontalAlignment="Left"
                                         Margin="2, 0, 0, 0">
                                    <Ellipse.RenderTransform>
                                        <TranslateTransform x:Name="ThumbTransform"/>
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                            </Grid>

                            <!-- Storyboard Animation -->
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="ToggleBackground" Property="Background" Value="#02a29a"/>
                                    <Setter Property="RenderTransform" TargetName="ToggleThumb">
                                        <Setter.Value>
                                            <TranslateTransform X="20"/>
                                            <!-- Adjusted movement -->
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="False">
                                    <Setter TargetName="ToggleBackground" Property="Background" Value="#d0dae1"/>
                                    <Setter Property="RenderTransform" TargetName="ToggleThumb">
                                        <Setter.Value>
                                            <TranslateTransform X="0"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>
                
                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                    
                    <TextBlock x:Name="Oven1BeltTextBlock" 
                               Text="Start/Stop"
                               Margin="125, 65, 0, 15"
                               Foreground="White" 
                               FontWeight="Bold" 
                               FontSize="12" 
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border CornerRadius="10"
                Background="Transparent"
                Width="230"
                Height="170"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="270,0,0,0">
            <Border.RenderTransform>
                <TranslateTransform Y="-40"/>
                <!-- Moves Up -->
            </Border.RenderTransform>

            <Grid>
                <!-- Dashed Border -->
                <Rectangle Stroke="White"
                           StrokeThickness="0.5"
                           StrokeDashArray="6 6"
                           RadiusX="10"
                           RadiusY="10"
                           Fill="Transparent" Margin="-19,-11,19,11"/>

                <!-- TextBlock Positioned at Top-Left -->
                <StackPanel Background="#157291"
                            HorizontalAlignment="Left" 
                            VerticalAlignment="Top" 
                            Margin="-5, -20, 0, 0">
                    <TextBlock Text="Oven 2"
                               Foreground="White"
                               FontSize="15"
                               FontWeight="Bold"
                               HorizontalAlignment="Left"
                               VerticalAlignment="Top"
                               Padding="5, 0"/>
                </StackPanel>


                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                    <TextBlock Text="Lamps" 
                               Margin="-16,5,0,0" 
                               Foreground="White" 
                               FontWeight="Bold" 
                               FontSize="12" 
                               VerticalAlignment="Center"/>
                </StackPanel>

                <ToggleButton x:Name="Oven2LampsToggle"
                              Width="40" Height="17" 
                              HorizontalAlignment="Left"
                              VerticalAlignment="Bottom"
                              Margin="85, 0, 0, 15"
                              Background="Transparent"
                              Cursor="Hand"
                              BorderThickness="0"
                              Click="Oven2LampsToggle_Click">
                    <ToggleButton.RenderTransform>
                        <TranslateTransform Y="-123"/>
                    </ToggleButton.RenderTransform>
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="ToggleButton">
                            <Grid Width="40" Height="17">
                                <!-- Adjusted Grid size -->
                                <!-- Background Border -->
                                <Border x:Name="ToggleBackground"
                                Width="40" Height="17" 
                                CornerRadius="8"
                                Background="Gray"/>

                                <!-- Toggle Thumb -->
                                <Ellipse x:Name="ToggleThumb"
                                 Width="15" Height="15"  
                                 Fill="White"
                                 HorizontalAlignment="Left"
                                 Margin="2, 0, 0, 0">
                                    <Ellipse.RenderTransform>
                                        <TranslateTransform x:Name="ThumbTransform"/>
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                            </Grid>

                            <!-- Storyboard Animation -->
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="ToggleBackground" Property="Background" Value="#02a29a"/>
                                    <Setter Property="RenderTransform" TargetName="ToggleThumb">
                                        <Setter.Value>
                                            <TranslateTransform X="20"/>
                                            <!-- Adjusted movement -->
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="False">
                                    <Setter TargetName="ToggleBackground" Property="Background" Value="#d0dae1"/>
                                    <Setter Property="RenderTransform" TargetName="ToggleThumb">
                                        <Setter.Value>
                                            <TranslateTransform X="0"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                    <TextBlock x:Name="Oven2LampsTextBlock" 
                               Text="On/Off"
                               Margin="125, 5, 0, 15"
                               Foreground="White" 
                               FontWeight="Bold" 
                               FontSize="12" 
                               VerticalAlignment="Center"/>
                </StackPanel>


                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                    <TextBlock Text="Fans" 
                               Margin="-16,35,0,0" 
                               Foreground="White" 
                               FontWeight="Bold" 
                               FontSize="12" 
                               VerticalAlignment="Center"/>
                </StackPanel>

                <ToggleButton x:Name="Oven2FansToggle"
                              Width="40" Height="17" 
                              HorizontalAlignment="Left"
                              VerticalAlignment="Bottom"
                              Margin="85, 0, 0, 15"
                              Background="Transparent"
                              Cursor="Hand"
                              BorderThickness="0"
                              Click="Oven2FansToggle_Click">
                    <ToggleButton.RenderTransform>
                        <TranslateTransform Y="-93"/>
                    </ToggleButton.RenderTransform>
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="ToggleButton">
                            <Grid Width="40" Height="17">
                                <!-- Adjusted Grid size -->
                                <!-- Background Border -->
                                <Border x:Name="ToggleBackground"
                                Width="40" Height="17" 
                                CornerRadius="8"
                                Background="Gray"/>

                                <!-- Toggle Thumb -->
                                <Ellipse x:Name="ToggleThumb"
                                 Width="15" Height="15"  
                                 Fill="White"
                                 HorizontalAlignment="Left"
                                 Margin="2, 0, 0, 0">
                                    <Ellipse.RenderTransform>
                                        <TranslateTransform x:Name="ThumbTransform"/>
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                            </Grid>

                            <!-- Storyboard Animation -->
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="ToggleBackground" Property="Background" Value="#02a29a"/>
                                    <Setter Property="RenderTransform" TargetName="ToggleThumb">
                                        <Setter.Value>
                                            <TranslateTransform X="20"/>
                                            <!-- Adjusted movement -->
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="False">
                                    <Setter TargetName="ToggleBackground" Property="Background" Value="#d0dae1"/>
                                    <Setter Property="RenderTransform" TargetName="ToggleThumb">
                                        <Setter.Value>
                                            <TranslateTransform X="0"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                    <TextBlock x:Name="Oven2FansTextBlock" 
                       Text="On/Off"
                       Margin="125, 35, 0, 15"
                       Foreground="White" 
                       FontWeight="Bold" 
                       FontSize="12" 
                       VerticalAlignment="Center"/>
                </StackPanel>


                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                    <TextBlock Text="Belt" 
                       Margin="-16,65,0,0" 
                       Foreground="White" 
                       FontWeight="Bold" 
                       FontSize="12" 
                       VerticalAlignment="Center"/>
                </StackPanel>

                <ToggleButton x:Name="Oven2BeltToggle"
                      Width="40" Height="17" 
                      HorizontalAlignment="Left"
                      VerticalAlignment="Bottom"
                      Margin="85, 0, 0, 15"
                      Background="Transparent"
                      Cursor="Hand"
                      BorderThickness="0"
                      Click="Oven2BeltToggle_Click">
                    <ToggleButton.RenderTransform>
                        <TranslateTransform Y="-63"/>
                    </ToggleButton.RenderTransform>
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="ToggleButton">
                            <Grid Width="40" Height="17">
                                <!-- Adjusted Grid size -->
                                <!-- Background Border -->
                                <Border x:Name="ToggleBackground"
                                Width="40" Height="17" 
                                CornerRadius="8"
                                Background="Gray"/>

                                <!-- Toggle Thumb -->
                                <Ellipse x:Name="ToggleThumb"
                                 Width="15" Height="15"  
                                 Fill="White"
                                 HorizontalAlignment="Left"
                                 Margin="2, 0, 0, 0">
                                    <Ellipse.RenderTransform>
                                        <TranslateTransform x:Name="ThumbTransform"/>
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                            </Grid>

                            <!-- Storyboard Animation -->
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="ToggleBackground" Property="Background" Value="#02a29a"/>
                                    <Setter Property="RenderTransform" TargetName="ToggleThumb">
                                        <Setter.Value>
                                            <TranslateTransform X="20"/>
                                            <!-- Adjusted movement -->
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="False">
                                    <Setter TargetName="ToggleBackground" Property="Background" Value="#d0dae1"/>
                                    <Setter Property="RenderTransform" TargetName="ToggleThumb">
                                        <Setter.Value>
                                            <TranslateTransform X="0"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">

                    <TextBlock x:Name="Oven2BeltTextBlock" 
                       Text="Start/Stop"
                       Margin="125, 65, 0, 15"
                       Foreground="White" 
                       FontWeight="Bold" 
                       FontSize="12" 
                       VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border CornerRadius="10"
                Background="Transparent"
                Width="500"
                Height="60"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="0,200,0,0">
            <Border.RenderTransform>
                <TranslateTransform Y="-40"/>
                <!-- Moves Up -->
            </Border.RenderTransform>

            <Grid>
                <!-- Dashed Border -->
                <Rectangle Stroke="White"
                           StrokeThickness="0.5"
                           StrokeDashArray="6 6"
                           RadiusX="10"
                           RadiusY="10"
                           Fill="Transparent" Margin="-19,-11,19,11"/>


                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                    <TextBlock Text="Input Belt" 
                               Margin="-16,5,0,0" 
                               Foreground="White" 
                               FontWeight="Bold" 
                               FontSize="12" 
                               VerticalAlignment="Center"/>
                </StackPanel>

                <ToggleButton x:Name="InputBeltToggle"
                              Width="40" Height="17" 
                              HorizontalAlignment="Left"
                              VerticalAlignment="Bottom"
                              Margin="90, 0, 0, 15"
                              Background="Transparent"
                              Cursor="Hand"
                              BorderThickness="0"
                              Click="InputBeltToggle_Click">
                    <ToggleButton.RenderTransform>
                        <TranslateTransform Y="-15"/>
                    </ToggleButton.RenderTransform>
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="ToggleButton">
                            <Grid Width="40" Height="17">
                                <!-- Adjusted Grid size -->
                                <!-- Background Border -->
                                <Border x:Name="ToggleBackground"
                                        Width="40" Height="17" 
                                        CornerRadius="8"
                                        Background="Gray"/>

                                <!-- Toggle Thumb -->
                                <Ellipse x:Name="ToggleThumb"
                                         Width="15" Height="15"  
                                         Fill="White"
                                         HorizontalAlignment="Left"
                                         Margin="2, 0, 0, 0">
                                    <Ellipse.RenderTransform>
                                        <TranslateTransform x:Name="ThumbTransform"/>
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                            </Grid>

                            <!-- Storyboard Animation -->
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="ToggleBackground" Property="Background" Value="#02a29a"/>
                                    <Setter Property="RenderTransform" TargetName="ToggleThumb">
                                        <Setter.Value>
                                            <TranslateTransform X="20"/>
                                            <!-- Adjusted movement -->
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="False">
                                    <Setter TargetName="ToggleBackground" Property="Background" Value="#d0dae1"/>
                                    <Setter Property="RenderTransform" TargetName="ToggleThumb">
                                        <Setter.Value>
                                            <TranslateTransform X="0"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                    <TextBlock x:Name="InputBeltTextBlock" 
                               Text="Start/Stop"
                               Margin="130, 5, 0, 15"
                               Foreground="White" 
                               FontWeight="Bold" 
                               FontSize="12" 
                               VerticalAlignment="Center"/>
                </StackPanel>


                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                    <TextBlock Text="Central Belt" 
                       Margin="250,5,0,0" 
                       Foreground="White" 
                       FontWeight="Bold" 
                       FontSize="12" 
                       VerticalAlignment="Center"/>
                </StackPanel>

                <ToggleButton x:Name="CentralBeltToggle"
                              Width="40" Height="17" 
                              HorizontalAlignment="Left"
                              VerticalAlignment="Bottom"
                              Margin="360, 0, 0, 15"
                              Background="Transparent"
                              Cursor="Hand"
                              BorderThickness="0"
                              Click="CentralBeltToggle_Click">
                    <ToggleButton.RenderTransform>
                        <TranslateTransform Y="-15"/>
                    </ToggleButton.RenderTransform>
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="ToggleButton">
                            <Grid Width="40" Height="17">
                                <!-- Adjusted Grid size -->
                                <!-- Background Border -->
                                <Border x:Name="ToggleBackground"
                                        Width="40" Height="17" 
                                        CornerRadius="8"
                                        Background="Gray"/>

                                <!-- Toggle Thumb -->
                                <Ellipse x:Name="ToggleThumb"
                                         Width="15" Height="15"  
                                         Fill="White"
                                         HorizontalAlignment="Left"
                                         Margin="2, 0, 0, 0">
                                    <Ellipse.RenderTransform>
                                        <TranslateTransform x:Name="ThumbTransform"/>
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                            </Grid>

                            <!-- Storyboard Animation -->
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="ToggleBackground" Property="Background" Value="#02a29a"/>
                                    <Setter Property="RenderTransform" TargetName="ToggleThumb">
                                        <Setter.Value>
                                            <TranslateTransform X="20"/>
                                            <!-- Adjusted movement -->
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="False">
                                    <Setter TargetName="ToggleBackground" Property="Background" Value="#d0dae1"/>
                                    <Setter Property="RenderTransform" TargetName="ToggleThumb">
                                        <Setter.Value>
                                            <TranslateTransform X="0"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                    <TextBlock x:Name="CentralBeltTextBlock" 
                               Text="Start/Stop"
                               Margin="400, 5, 0, 15"
                               Foreground="White" 
                               FontWeight="Bold" 
                               FontSize="12" 
                               VerticalAlignment="Center"/>
                </StackPanel>

            </Grid>
        </Border>

        <Border x:Name="MessageBoxPanel"
                CornerRadius="5"
                Background="Transparent"
                Width="450"
                Height="40"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Visibility="Collapsed"
                Margin="0,250,0,0">
            <Border.RenderTransform>
                <TransformGroup>
                    <TranslateTransform X="80" />
                    <TranslateTransform Y="70" />
                </TransformGroup>
            </Border.RenderTransform>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" Grid.Column="0">
                    <Image x:Name="MessageIcon" 
                           Width="20" 
                           Height="20" 
                           Margin="10,5,5,5"/>
                    <TextBlock x:Name="MessageText" 
                               FontSize="14" 
                               VerticalAlignment="Center" 
                               Margin="5,5,20,5" 
                               TextWrapping="Wrap"/>
                </StackPanel>

                <!-- Close Button -->
                <Button Content="✖"
                        Background="Transparent"
                        Foreground="Gray"
                        BorderThickness="0"
                        FontSize="10"
                        Cursor="Hand"
                        Margin="5,0,10,0"
                        Click="CloseMessageBox"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Grid.Column="1"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>